name: ppo_package_analytics
description: Analytics plugin for Positive Plus One
version: 0.0.1
publish_to: none
homepage: www.positiveplusone.com

environment:
  sdk: ">=2.17.5 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  # State and Management
  inqvine_core_main: ^1.3.2

  # Core Services
  mixpanel_flutter: 1.6.0
  flutter_secure_storage: 5.0.2

  # Testing
  mocktail: 0.3.0
  ppo_package_test:
    path: ../ppo_package_test

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Linting and Static Analysis
  flutter_lints: 2.0.1

flutter: