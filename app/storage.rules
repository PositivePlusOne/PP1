rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /flamelink/{allPaths=**} {
      allow read: if request.auth != null && request.auth.token.claims.admin == 'true';
      allow write: if request.auth != null && request.auth.token.claims.admin == 'true';
    }
  }
}
