export enum RelationshipState {
    sourceBlocked,
    sourceConnected,
    sourceFollowed,
    sourceHidden,
    sourceMuted,
    targetBlocked,
    targetConnected,
    targetFollowing,
    targetHidden,
    targetMuted,
}